[project]
name = "http-gate"
version = "0.0.0"
authors = [{ name = "David Liu" }]
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "redis[asyncio]",
]

[dependency-groups]
dev = [
    "pytest"
]

